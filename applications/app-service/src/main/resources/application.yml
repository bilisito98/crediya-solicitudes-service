server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://dpg-d2n56fqli9vc73bnvas0-a/creditya_db?sslmode=require
    username: creditya_db_user
    password: NxQrf1h15WeUU61fVpAFRersiAuGQQSf
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  main:
    banner-mode: console

logging:
  level:
    org.flywaydb: info
    org.springframework.r2dbc: info
    sql: debug

springdoc:
  swagger-ui:
    path: /swagger-ui.html
